# Pointcloud database
PGHOST=localhost
PGPORT=5432
PGUSER=pointcloud
PGPASSWORD=changeme
PGDATABASE=pointcloud

# Pointcloud + routing service
POINTCLOUD_URL=http://localhost:8300
ROUTE_CORPUS_SLUG=skills

# Embedding provider
OPENAI_API_KEY=sk-replace-me
EMBED_MODEL=text-embedding-3-small

# Governance log (local SQLite)
GOVERNANCE_DB=./data/governance.db

# Optional: outbound HTTP timeout (seconds)
HTTP_TIMEOUT=10
